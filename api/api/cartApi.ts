/**
 * Ecommerce Demo API
 * Stable, codegen-oriented OpenAPI contract. Designed for multi-project, multi-language use (client/server stubs, DTOs, SDKs).
 *
 * The version of the OpenAPI document: 2.0.0
 *
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */

import localVarRequest from 'request';
import http from 'node:http';

/* tslint:disable:no-unused-locals */
import { CartResponse } from '../model/cartResponse';
import { CartSummaryResponse } from '../model/cartSummaryResponse';
import { ErrorResponse } from '../model/errorResponse';
import { RemoveCartItemRequest } from '../model/removeCartItemRequest';
import { UpdateCartItemByIdRequest } from '../model/updateCartItemByIdRequest';
import { UpsertCartItemRequest } from '../model/upsertCartItemRequest';
import { ValidationErrorResponse } from '../model/validationErrorResponse';

import { ObjectSerializer, Authentication, VoidAuth, Interceptor } from '../model/models';
import { HttpBasicAuth, HttpBearerAuth, ApiKeyAuth, OAuth } from '../model/models';

import { HttpError, RequestFile } from './apis';

const defaultBasePath = 'http://localhost:3000';

// ===============================================
// This file is autogenerated - Please do not edit
// ===============================================

export enum CartApiApiKeys {}

export class CartApi {
    protected _basePath = defaultBasePath;
    protected _defaultHeaders: any = {};
    protected _useQuerystring: boolean = false;

    protected authentications = {
        default: <Authentication>new VoidAuth(),
        bearerAuth: new HttpBearerAuth()
    };

    protected interceptors: Interceptor[] = [];

    constructor(basePath?: string);
    constructor(basePathOrUsername: string, password?: string, basePath?: string) {
        if (password) {
            if (basePath) {
                this.basePath = basePath;
            }
        } else {
            if (basePathOrUsername) {
                this.basePath = basePathOrUsername;
            }
        }
    }

    set useQuerystring(value: boolean) {
        this._useQuerystring = value;
    }

    set basePath(basePath: string) {
        this._basePath = basePath;
    }

    set defaultHeaders(defaultHeaders: any) {
        this._defaultHeaders = defaultHeaders;
    }

    get defaultHeaders() {
        return this._defaultHeaders;
    }

    get basePath() {
        return this._basePath;
    }

    public setDefaultAuthentication(auth: Authentication) {
        this.authentications.default = auth;
    }

    public setApiKey(key: CartApiApiKeys, value: string) {
        (this.authentications as any)[CartApiApiKeys[key]].apiKey = value;
    }

    set accessToken(accessToken: string | (() => string)) {
        this.authentications.bearerAuth.accessToken = accessToken;
    }

    public addInterceptor(interceptor: Interceptor) {
        this.interceptors.push(interceptor);
    }

    /**
     * Clear cart or, ir productId is set, removes a specific product from the authenticated user\'s cart. Returns the updated cart (can be empty)
     * @summary Empty cart or, if productId is set, remove target cart item
     * @param removeCartItemRequest
     */
    public async clearCart(
        removeCartItemRequest?: RemoveCartItemRequest,
        options: { headers: { [name: string]: string } } = { headers: {} }
    ): Promise<{ response: http.IncomingMessage; body: CartResponse }> {
        const localVariablePath = this.basePath + '/cart';
        const localVariableQueryParameters: any = {};
        const localVariableHeaderParameters: any = (<any>Object).assign({}, this._defaultHeaders);
        const produces = ['application/json'];
        // give precedence to 'application/json'
        localVariableHeaderParameters.Accept = produces.includes('application/json')
            ? 'application/json'
            : produces.join(',');
        const localVariableFormParameters: any = {};

        (<any>Object).assign(localVariableHeaderParameters, options.headers);

        const localVariableUseFormData = false;

        const localVariableRequestOptions: localVarRequest.Options = {
            method: 'DELETE',
            qs: localVariableQueryParameters,
            headers: localVariableHeaderParameters,
            uri: localVariablePath,
            useQuerystring: this._useQuerystring,
            json: true,
            body: ObjectSerializer.serialize(removeCartItemRequest, 'RemoveCartItemRequest')
        };

        let authenticationPromise = Promise.resolve();
        if (this.authentications.bearerAuth.accessToken) {
            authenticationPromise = authenticationPromise.then(() =>
                this.authentications.bearerAuth.applyToRequest(localVariableRequestOptions)
            );
        }
        authenticationPromise = authenticationPromise.then(() =>
            this.authentications.default.applyToRequest(localVariableRequestOptions)
        );

        let interceptorPromise = authenticationPromise;
        for (const interceptor of this.interceptors) {
            interceptorPromise = interceptorPromise.then(() =>
                interceptor(localVariableRequestOptions)
            );
        }

        return interceptorPromise.then(() => {
            if (Object.keys(localVariableFormParameters).length > 0) {
                if (localVariableUseFormData) {
                    (<any>localVariableRequestOptions).formData = localVariableFormParameters;
                } else {
                    localVariableRequestOptions.form = localVariableFormParameters;
                }
            }
            return new Promise<{ response: http.IncomingMessage; body: CartResponse }>(
                (resolve, reject) => {
                    localVarRequest(localVariableRequestOptions, (error, response, body) => {
                        if (error) {
                            reject(error);
                        } else {
                            if (
                                response.statusCode &&
                                response.statusCode >= 200 &&
                                response.statusCode <= 299
                            ) {
                                body = ObjectSerializer.deserialize(body, 'CartResponse');
                                resolve({ response: response, body: body });
                            } else {
                                reject(new HttpError(response, body, response.statusCode));
                            }
                        }
                    });
                }
            );
        });
    }
    /**
     * Returns all items currently in the authenticated user\'s cart along with a computed summary
     * @summary Get cart
     */
    public async getCart(
        options: { headers: { [name: string]: string } } = { headers: {} }
    ): Promise<{ response: http.IncomingMessage; body: CartResponse }> {
        const localVariablePath = this.basePath + '/cart';
        const localVariableQueryParameters: any = {};
        const localVariableHeaderParameters: any = (<any>Object).assign({}, this._defaultHeaders);
        const produces = ['application/json'];
        // give precedence to 'application/json'
        localVariableHeaderParameters.Accept = produces.includes('application/json')
            ? 'application/json'
            : produces.join(',');
        const localVariableFormParameters: any = {};

        (<any>Object).assign(localVariableHeaderParameters, options.headers);

        const localVariableUseFormData = false;

        const localVariableRequestOptions: localVarRequest.Options = {
            method: 'GET',
            qs: localVariableQueryParameters,
            headers: localVariableHeaderParameters,
            uri: localVariablePath,
            useQuerystring: this._useQuerystring,
            json: true
        };

        let authenticationPromise = Promise.resolve();
        if (this.authentications.bearerAuth.accessToken) {
            authenticationPromise = authenticationPromise.then(() =>
                this.authentications.bearerAuth.applyToRequest(localVariableRequestOptions)
            );
        }
        authenticationPromise = authenticationPromise.then(() =>
            this.authentications.default.applyToRequest(localVariableRequestOptions)
        );

        let interceptorPromise = authenticationPromise;
        for (const interceptor of this.interceptors) {
            interceptorPromise = interceptorPromise.then(() =>
                interceptor(localVariableRequestOptions)
            );
        }

        return interceptorPromise.then(() => {
            if (Object.keys(localVariableFormParameters).length > 0) {
                if (localVariableUseFormData) {
                    (<any>localVariableRequestOptions).formData = localVariableFormParameters;
                } else {
                    localVariableRequestOptions.form = localVariableFormParameters;
                }
            }
            return new Promise<{ response: http.IncomingMessage; body: CartResponse }>(
                (resolve, reject) => {
                    localVarRequest(localVariableRequestOptions, (error, response, body) => {
                        if (error) {
                            reject(error);
                        } else {
                            if (
                                response.statusCode &&
                                response.statusCode >= 200 &&
                                response.statusCode <= 299
                            ) {
                                body = ObjectSerializer.deserialize(body, 'CartResponse');
                                resolve({ response: response, body: body });
                            } else {
                                reject(new HttpError(response, body, response.statusCode));
                            }
                        }
                    });
                }
            );
        });
    }
    /**
     * Returns a lightweight summary of the authenticated user\'s cart.
     * @summary Get cart summary
     */
    public async getCartSummary(
        options: { headers: { [name: string]: string } } = { headers: {} }
    ): Promise<{ response: http.IncomingMessage; body: CartSummaryResponse }> {
        const localVariablePath = this.basePath + '/cart/summary';
        const localVariableQueryParameters: any = {};
        const localVariableHeaderParameters: any = (<any>Object).assign({}, this._defaultHeaders);
        const produces = ['application/json'];
        // give precedence to 'application/json'
        localVariableHeaderParameters.Accept = produces.includes('application/json')
            ? 'application/json'
            : produces.join(',');
        const localVariableFormParameters: any = {};

        (<any>Object).assign(localVariableHeaderParameters, options.headers);

        const localVariableUseFormData = false;

        const localVariableRequestOptions: localVarRequest.Options = {
            method: 'GET',
            qs: localVariableQueryParameters,
            headers: localVariableHeaderParameters,
            uri: localVariablePath,
            useQuerystring: this._useQuerystring,
            json: true
        };

        let authenticationPromise = Promise.resolve();
        if (this.authentications.bearerAuth.accessToken) {
            authenticationPromise = authenticationPromise.then(() =>
                this.authentications.bearerAuth.applyToRequest(localVariableRequestOptions)
            );
        }
        authenticationPromise = authenticationPromise.then(() =>
            this.authentications.default.applyToRequest(localVariableRequestOptions)
        );

        let interceptorPromise = authenticationPromise;
        for (const interceptor of this.interceptors) {
            interceptorPromise = interceptorPromise.then(() =>
                interceptor(localVariableRequestOptions)
            );
        }

        return interceptorPromise.then(() => {
            if (Object.keys(localVariableFormParameters).length > 0) {
                if (localVariableUseFormData) {
                    (<any>localVariableRequestOptions).formData = localVariableFormParameters;
                } else {
                    localVariableRequestOptions.form = localVariableFormParameters;
                }
            }
            return new Promise<{ response: http.IncomingMessage; body: CartSummaryResponse }>(
                (resolve, reject) => {
                    localVarRequest(localVariableRequestOptions, (error, response, body) => {
                        if (error) {
                            reject(error);
                        } else {
                            if (
                                response.statusCode &&
                                response.statusCode >= 200 &&
                                response.statusCode <= 299
                            ) {
                                body = ObjectSerializer.deserialize(body, 'CartSummaryResponse');
                                resolve({ response: response, body: body });
                            } else {
                                reject(new HttpError(response, body, response.statusCode));
                            }
                        }
                    });
                }
            );
        });
    }
    /**
     * Removes the cart line for the product identified by `{productId}` in the path from the authenticated user\'s cart. Returns the updated cart.
     * @summary Remove item from cart
     * @param productId Product identifier
     */
    public async removeCartItem(
        productId: string,
        options: { headers: { [name: string]: string } } = { headers: {} }
    ): Promise<{ response: http.IncomingMessage; body: CartResponse }> {
        const localVariablePath =
            this.basePath +
            '/cart/{productId}'.replace(
                '{' + 'productId' + '}',
                encodeURIComponent(String(productId))
            );
        const localVariableQueryParameters: any = {};
        const localVariableHeaderParameters: any = (<any>Object).assign({}, this._defaultHeaders);
        const produces = ['application/json'];
        // give precedence to 'application/json'
        localVariableHeaderParameters.Accept = produces.includes('application/json')
            ? 'application/json'
            : produces.join(',');
        const localVariableFormParameters: any = {};

        // verify required parameter 'productId' is not null or undefined
        if (productId === null || productId === undefined) {
            throw new Error(
                'Required parameter productId was null or undefined when calling removeCartItem.'
            );
        }

        (<any>Object).assign(localVariableHeaderParameters, options.headers);

        const localVariableUseFormData = false;

        const localVariableRequestOptions: localVarRequest.Options = {
            method: 'DELETE',
            qs: localVariableQueryParameters,
            headers: localVariableHeaderParameters,
            uri: localVariablePath,
            useQuerystring: this._useQuerystring,
            json: true
        };

        let authenticationPromise = Promise.resolve();
        if (this.authentications.bearerAuth.accessToken) {
            authenticationPromise = authenticationPromise.then(() =>
                this.authentications.bearerAuth.applyToRequest(localVariableRequestOptions)
            );
        }
        authenticationPromise = authenticationPromise.then(() =>
            this.authentications.default.applyToRequest(localVariableRequestOptions)
        );

        let interceptorPromise = authenticationPromise;
        for (const interceptor of this.interceptors) {
            interceptorPromise = interceptorPromise.then(() =>
                interceptor(localVariableRequestOptions)
            );
        }

        return interceptorPromise.then(() => {
            if (Object.keys(localVariableFormParameters).length > 0) {
                if (localVariableUseFormData) {
                    (<any>localVariableRequestOptions).formData = localVariableFormParameters;
                } else {
                    localVariableRequestOptions.form = localVariableFormParameters;
                }
            }
            return new Promise<{ response: http.IncomingMessage; body: CartResponse }>(
                (resolve, reject) => {
                    localVarRequest(localVariableRequestOptions, (error, response, body) => {
                        if (error) {
                            reject(error);
                        } else {
                            if (
                                response.statusCode &&
                                response.statusCode >= 200 &&
                                response.statusCode <= 299
                            ) {
                                body = ObjectSerializer.deserialize(body, 'CartResponse');
                                resolve({ response: response, body: body });
                            } else {
                                reject(new HttpError(response, body, response.statusCode));
                            }
                        }
                    });
                }
            );
        });
    }
    /**
     * Sets the quantity of the cart line for the product identified by `{productId}` in the path. Functionally equivalent to `POST /cart`. Returns the updated cart.
     * @summary Set cart item quantity
     * @param productId Product identifier
     * @param updateCartItemByIdRequest
     */
    public async updateCartItemById(
        productId: string,
        updateCartItemByIdRequest: UpdateCartItemByIdRequest,
        options: { headers: { [name: string]: string } } = { headers: {} }
    ): Promise<{ response: http.IncomingMessage; body: CartResponse }> {
        const localVariablePath =
            this.basePath +
            '/cart/{productId}'.replace(
                '{' + 'productId' + '}',
                encodeURIComponent(String(productId))
            );
        const localVariableQueryParameters: any = {};
        const localVariableHeaderParameters: any = (<any>Object).assign({}, this._defaultHeaders);
        const produces = ['application/json'];
        // give precedence to 'application/json'
        localVariableHeaderParameters.Accept = produces.includes('application/json')
            ? 'application/json'
            : produces.join(',');
        const localVariableFormParameters: any = {};

        // verify required parameter 'productId' is not null or undefined
        if (productId === null || productId === undefined) {
            throw new Error(
                'Required parameter productId was null or undefined when calling updateCartItemById.'
            );
        }

        // verify required parameter 'updateCartItemByIdRequest' is not null or undefined
        if (updateCartItemByIdRequest === null || updateCartItemByIdRequest === undefined) {
            throw new Error(
                'Required parameter updateCartItemByIdRequest was null or undefined when calling updateCartItemById.'
            );
        }

        (<any>Object).assign(localVariableHeaderParameters, options.headers);

        const localVariableUseFormData = false;

        const localVariableRequestOptions: localVarRequest.Options = {
            method: 'PUT',
            qs: localVariableQueryParameters,
            headers: localVariableHeaderParameters,
            uri: localVariablePath,
            useQuerystring: this._useQuerystring,
            json: true,
            body: ObjectSerializer.serialize(updateCartItemByIdRequest, 'UpdateCartItemByIdRequest')
        };

        let authenticationPromise = Promise.resolve();
        if (this.authentications.bearerAuth.accessToken) {
            authenticationPromise = authenticationPromise.then(() =>
                this.authentications.bearerAuth.applyToRequest(localVariableRequestOptions)
            );
        }
        authenticationPromise = authenticationPromise.then(() =>
            this.authentications.default.applyToRequest(localVariableRequestOptions)
        );

        let interceptorPromise = authenticationPromise;
        for (const interceptor of this.interceptors) {
            interceptorPromise = interceptorPromise.then(() =>
                interceptor(localVariableRequestOptions)
            );
        }

        return interceptorPromise.then(() => {
            if (Object.keys(localVariableFormParameters).length > 0) {
                if (localVariableUseFormData) {
                    (<any>localVariableRequestOptions).formData = localVariableFormParameters;
                } else {
                    localVariableRequestOptions.form = localVariableFormParameters;
                }
            }
            return new Promise<{ response: http.IncomingMessage; body: CartResponse }>(
                (resolve, reject) => {
                    localVarRequest(localVariableRequestOptions, (error, response, body) => {
                        if (error) {
                            reject(error);
                        } else {
                            if (
                                response.statusCode &&
                                response.statusCode >= 200 &&
                                response.statusCode <= 299
                            ) {
                                body = ObjectSerializer.deserialize(body, 'CartResponse');
                                resolve({ response: response, body: body });
                            } else {
                                reject(new HttpError(response, body, response.statusCode));
                            }
                        }
                    });
                }
            );
        });
    }
    /**
     * Adds or edit a product to the authenticated user\'s cart. Returns the updated cart.
     * @summary Add/Edit cart item
     * @param upsertCartItemRequest
     */
    public async upsertCartItem(
        upsertCartItemRequest: UpsertCartItemRequest,
        options: { headers: { [name: string]: string } } = { headers: {} }
    ): Promise<{ response: http.IncomingMessage; body: CartResponse }> {
        const localVariablePath = this.basePath + '/cart';
        const localVariableQueryParameters: any = {};
        const localVariableHeaderParameters: any = (<any>Object).assign({}, this._defaultHeaders);
        const produces = ['application/json'];
        // give precedence to 'application/json'
        localVariableHeaderParameters.Accept = produces.includes('application/json')
            ? 'application/json'
            : produces.join(',');
        const localVariableFormParameters: any = {};

        // verify required parameter 'upsertCartItemRequest' is not null or undefined
        if (upsertCartItemRequest === null || upsertCartItemRequest === undefined) {
            throw new Error(
                'Required parameter upsertCartItemRequest was null or undefined when calling upsertCartItem.'
            );
        }

        (<any>Object).assign(localVariableHeaderParameters, options.headers);

        const localVariableUseFormData = false;

        const localVariableRequestOptions: localVarRequest.Options = {
            method: 'POST',
            qs: localVariableQueryParameters,
            headers: localVariableHeaderParameters,
            uri: localVariablePath,
            useQuerystring: this._useQuerystring,
            json: true,
            body: ObjectSerializer.serialize(upsertCartItemRequest, 'UpsertCartItemRequest')
        };

        let authenticationPromise = Promise.resolve();
        if (this.authentications.bearerAuth.accessToken) {
            authenticationPromise = authenticationPromise.then(() =>
                this.authentications.bearerAuth.applyToRequest(localVariableRequestOptions)
            );
        }
        authenticationPromise = authenticationPromise.then(() =>
            this.authentications.default.applyToRequest(localVariableRequestOptions)
        );

        let interceptorPromise = authenticationPromise;
        for (const interceptor of this.interceptors) {
            interceptorPromise = interceptorPromise.then(() =>
                interceptor(localVariableRequestOptions)
            );
        }

        return interceptorPromise.then(() => {
            if (Object.keys(localVariableFormParameters).length > 0) {
                if (localVariableUseFormData) {
                    (<any>localVariableRequestOptions).formData = localVariableFormParameters;
                } else {
                    localVariableRequestOptions.form = localVariableFormParameters;
                }
            }
            return new Promise<{ response: http.IncomingMessage; body: CartResponse }>(
                (resolve, reject) => {
                    localVarRequest(localVariableRequestOptions, (error, response, body) => {
                        if (error) {
                            reject(error);
                        } else {
                            if (
                                response.statusCode &&
                                response.statusCode >= 200 &&
                                response.statusCode <= 299
                            ) {
                                body = ObjectSerializer.deserialize(body, 'CartResponse');
                                resolve({ response: response, body: body });
                            } else {
                                reject(new HttpError(response, body, response.statusCode));
                            }
                        }
                    });
                }
            );
        });
    }
}
