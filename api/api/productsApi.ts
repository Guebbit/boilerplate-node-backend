/**
 * Ecommerce Demo API
 * Stable, codegen-oriented OpenAPI contract. Designed for multi-project, multi-language use (client/server stubs, DTOs, SDKs).
 *
 * The version of the OpenAPI document: 2.0.0
 *
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */

import localVarRequest from 'request';
import http from 'node:http';

/* tslint:disable:no-unused-locals */
import { DeleteProductRequest } from '../model/deleteProductRequest';
import { ErrorResponse } from '../model/errorResponse';
import { MessageResponse } from '../model/messageResponse';
import { Product } from '../model/product';
import { ProductsResponse } from '../model/productsResponse';
import { SearchProductsRequest } from '../model/searchProductsRequest';
import { ValidationErrorResponse } from '../model/validationErrorResponse';

import { ObjectSerializer, Authentication, VoidAuth, Interceptor } from '../model/models';
import { HttpBasicAuth, HttpBearerAuth, ApiKeyAuth, OAuth } from '../model/models';

import { HttpError, RequestFile } from './apis';

const defaultBasePath = 'http://localhost:3000';

// ===============================================
// This file is autogenerated - Please do not edit
// ===============================================

export enum ProductsApiApiKeys {}

export class ProductsApi {
    protected _basePath = defaultBasePath;
    protected _defaultHeaders: any = {};
    protected _useQuerystring: boolean = false;

    protected authentications = {
        default: <Authentication>new VoidAuth(),
        bearerAuth: new HttpBearerAuth()
    };

    protected interceptors: Interceptor[] = [];

    constructor(basePath?: string);
    constructor(basePathOrUsername: string, password?: string, basePath?: string) {
        if (password) {
            if (basePath) {
                this.basePath = basePath;
            }
        } else {
            if (basePathOrUsername) {
                this.basePath = basePathOrUsername;
            }
        }
    }

    set useQuerystring(value: boolean) {
        this._useQuerystring = value;
    }

    set basePath(basePath: string) {
        this._basePath = basePath;
    }

    set defaultHeaders(defaultHeaders: any) {
        this._defaultHeaders = defaultHeaders;
    }

    get defaultHeaders() {
        return this._defaultHeaders;
    }

    get basePath() {
        return this._basePath;
    }

    public setDefaultAuthentication(auth: Authentication) {
        this.authentications.default = auth;
    }

    public setApiKey(key: ProductsApiApiKeys, value: string) {
        (this.authentications as any)[ProductsApiApiKeys[key]].apiKey = value;
    }

    set accessToken(accessToken: string | (() => string)) {
        this.authentications.bearerAuth.accessToken = accessToken;
    }

    public addInterceptor(interceptor: Interceptor) {
        this.interceptors.push(interceptor);
    }

    /**
     * Creates a new product with optional image upload
     * @summary Create product
     * @param title
     * @param price
     * @param description
     * @param active
     * @param imageUpload Optional product image
     */
    public async createProduct(
        title: string,
        price: number,
        description?: string,
        active?: boolean,
        imageUpload?: RequestFile,
        options: { headers: { [name: string]: string } } = { headers: {} }
    ): Promise<{ response: http.IncomingMessage; body: Product }> {
        const localVariablePath = this.basePath + '/products';
        const localVariableQueryParameters: any = {};
        const localVariableHeaderParameters: any = (<any>Object).assign({}, this._defaultHeaders);
        const produces = ['application/json'];
        // give precedence to 'application/json'
        localVariableHeaderParameters.Accept = produces.includes('application/json')
            ? 'application/json'
            : produces.join(',');
        const localVariableFormParameters: any = {};

        // verify required parameter 'title' is not null or undefined
        if (title === null || title === undefined) {
            throw new Error(
                'Required parameter title was null or undefined when calling createProduct.'
            );
        }

        // verify required parameter 'price' is not null or undefined
        if (price === null || price === undefined) {
            throw new Error(
                'Required parameter price was null or undefined when calling createProduct.'
            );
        }

        (<any>Object).assign(localVariableHeaderParameters, options.headers);

        let localVariableUseFormData = false;

        if (title !== undefined) {
            localVariableFormParameters['title'] = ObjectSerializer.serialize(title, 'string');
        }

        if (price !== undefined) {
            localVariableFormParameters['price'] = ObjectSerializer.serialize(price, 'number');
        }

        if (description !== undefined) {
            localVariableFormParameters['description'] = ObjectSerializer.serialize(
                description,
                'string'
            );
        }

        if (active !== undefined) {
            localVariableFormParameters['active'] = ObjectSerializer.serialize(active, 'boolean');
        }

        if (imageUpload !== undefined) {
            localVariableFormParameters['imageUpload'] = imageUpload;
        }
        localVariableUseFormData = true;

        const localVariableRequestOptions: localVarRequest.Options = {
            method: 'POST',
            qs: localVariableQueryParameters,
            headers: localVariableHeaderParameters,
            uri: localVariablePath,
            useQuerystring: this._useQuerystring,
            json: true
        };

        let authenticationPromise = Promise.resolve();
        if (this.authentications.bearerAuth.accessToken) {
            authenticationPromise = authenticationPromise.then(() =>
                this.authentications.bearerAuth.applyToRequest(localVariableRequestOptions)
            );
        }
        authenticationPromise = authenticationPromise.then(() =>
            this.authentications.default.applyToRequest(localVariableRequestOptions)
        );

        let interceptorPromise = authenticationPromise;
        for (const interceptor of this.interceptors) {
            interceptorPromise = interceptorPromise.then(() =>
                interceptor(localVariableRequestOptions)
            );
        }

        return interceptorPromise.then(() => {
            if (Object.keys(localVariableFormParameters).length > 0) {
                if (localVariableUseFormData) {
                    (<any>localVariableRequestOptions).formData = localVariableFormParameters;
                } else {
                    localVariableRequestOptions.form = localVariableFormParameters;
                }
            }
            return new Promise<{ response: http.IncomingMessage; body: Product }>(
                (resolve, reject) => {
                    localVarRequest(localVariableRequestOptions, (error, response, body) => {
                        if (error) {
                            reject(error);
                        } else {
                            if (
                                response.statusCode &&
                                response.statusCode >= 200 &&
                                response.statusCode <= 299
                            ) {
                                body = ObjectSerializer.deserialize(body, 'Product');
                                resolve({ response: response, body: body });
                            } else {
                                reject(new HttpError(response, body, response.statusCode));
                            }
                        }
                    });
                }
            );
        });
    }
    /**
     * Deletes the product identified by the `id` field in the request body. Set `hardDelete` to `true` to permanently remove the record
     * @summary Delete product
     * @param deleteProductRequest
     */
    public async deleteProduct(
        deleteProductRequest: DeleteProductRequest,
        options: { headers: { [name: string]: string } } = { headers: {} }
    ): Promise<{ response: http.IncomingMessage; body: MessageResponse }> {
        const localVariablePath = this.basePath + '/products';
        const localVariableQueryParameters: any = {};
        const localVariableHeaderParameters: any = (<any>Object).assign({}, this._defaultHeaders);
        const produces = ['application/json'];
        // give precedence to 'application/json'
        localVariableHeaderParameters.Accept = produces.includes('application/json')
            ? 'application/json'
            : produces.join(',');
        const localVariableFormParameters: any = {};

        // verify required parameter 'deleteProductRequest' is not null or undefined
        if (deleteProductRequest === null || deleteProductRequest === undefined) {
            throw new Error(
                'Required parameter deleteProductRequest was null or undefined when calling deleteProduct.'
            );
        }

        (<any>Object).assign(localVariableHeaderParameters, options.headers);

        const localVariableUseFormData = false;

        const localVariableRequestOptions: localVarRequest.Options = {
            method: 'DELETE',
            qs: localVariableQueryParameters,
            headers: localVariableHeaderParameters,
            uri: localVariablePath,
            useQuerystring: this._useQuerystring,
            json: true,
            body: ObjectSerializer.serialize(deleteProductRequest, 'DeleteProductRequest')
        };

        let authenticationPromise = Promise.resolve();
        if (this.authentications.bearerAuth.accessToken) {
            authenticationPromise = authenticationPromise.then(() =>
                this.authentications.bearerAuth.applyToRequest(localVariableRequestOptions)
            );
        }
        authenticationPromise = authenticationPromise.then(() =>
            this.authentications.default.applyToRequest(localVariableRequestOptions)
        );

        let interceptorPromise = authenticationPromise;
        for (const interceptor of this.interceptors) {
            interceptorPromise = interceptorPromise.then(() =>
                interceptor(localVariableRequestOptions)
            );
        }

        return interceptorPromise.then(() => {
            if (Object.keys(localVariableFormParameters).length > 0) {
                if (localVariableUseFormData) {
                    (<any>localVariableRequestOptions).formData = localVariableFormParameters;
                } else {
                    localVariableRequestOptions.form = localVariableFormParameters;
                }
            }
            return new Promise<{ response: http.IncomingMessage; body: MessageResponse }>(
                (resolve, reject) => {
                    localVarRequest(localVariableRequestOptions, (error, response, body) => {
                        if (error) {
                            reject(error);
                        } else {
                            if (
                                response.statusCode &&
                                response.statusCode >= 200 &&
                                response.statusCode <= 299
                            ) {
                                body = ObjectSerializer.deserialize(body, 'MessageResponse');
                                resolve({ response: response, body: body });
                            } else {
                                reject(new HttpError(response, body, response.statusCode));
                            }
                        }
                    });
                }
            );
        });
    }
    /**
     * Deletes the product identified by `{id}` in the path. Pass the `hardDelete` query parameter as `true` to permanently remove the record. Functionally equivalent to `DELETE /products`.
     * @summary Delete product
     * @param id Resource identifier
     * @param hardDelete
     */
    public async deleteProductById(
        id: string,
        hardDelete?: boolean,
        options: { headers: { [name: string]: string } } = { headers: {} }
    ): Promise<{ response: http.IncomingMessage; body: MessageResponse }> {
        const localVariablePath =
            this.basePath +
            '/products/{id}'.replace('{' + 'id' + '}', encodeURIComponent(String(id)));
        const localVariableQueryParameters: any = {};
        const localVariableHeaderParameters: any = (<any>Object).assign({}, this._defaultHeaders);
        const produces = ['application/json'];
        // give precedence to 'application/json'
        localVariableHeaderParameters.Accept = produces.includes('application/json')
            ? 'application/json'
            : produces.join(',');
        const localVariableFormParameters: any = {};

        // verify required parameter 'id' is not null or undefined
        if (id === null || id === undefined) {
            throw new Error(
                'Required parameter id was null or undefined when calling deleteProductById.'
            );
        }

        if (hardDelete !== undefined) {
            localVariableQueryParameters['hardDelete'] = ObjectSerializer.serialize(
                hardDelete,
                'boolean'
            );
        }

        (<any>Object).assign(localVariableHeaderParameters, options.headers);

        const localVariableUseFormData = false;

        const localVariableRequestOptions: localVarRequest.Options = {
            method: 'DELETE',
            qs: localVariableQueryParameters,
            headers: localVariableHeaderParameters,
            uri: localVariablePath,
            useQuerystring: this._useQuerystring,
            json: true
        };

        let authenticationPromise = Promise.resolve();
        if (this.authentications.bearerAuth.accessToken) {
            authenticationPromise = authenticationPromise.then(() =>
                this.authentications.bearerAuth.applyToRequest(localVariableRequestOptions)
            );
        }
        authenticationPromise = authenticationPromise.then(() =>
            this.authentications.default.applyToRequest(localVariableRequestOptions)
        );

        let interceptorPromise = authenticationPromise;
        for (const interceptor of this.interceptors) {
            interceptorPromise = interceptorPromise.then(() =>
                interceptor(localVariableRequestOptions)
            );
        }

        return interceptorPromise.then(() => {
            if (Object.keys(localVariableFormParameters).length > 0) {
                if (localVariableUseFormData) {
                    (<any>localVariableRequestOptions).formData = localVariableFormParameters;
                } else {
                    localVariableRequestOptions.form = localVariableFormParameters;
                }
            }
            return new Promise<{ response: http.IncomingMessage; body: MessageResponse }>(
                (resolve, reject) => {
                    localVarRequest(localVariableRequestOptions, (error, response, body) => {
                        if (error) {
                            reject(error);
                        } else {
                            if (
                                response.statusCode &&
                                response.statusCode >= 200 &&
                                response.statusCode <= 299
                            ) {
                                body = ObjectSerializer.deserialize(body, 'MessageResponse');
                                resolve({ response: response, body: body });
                            } else {
                                reject(new HttpError(response, body, response.statusCode));
                            }
                        }
                    });
                }
            );
        });
    }
    /**
     * Returns the full details of the product identified by `{id}`. Functionally equivalent to `GET /products?id={id}`.
     * @summary Product details
     * @param id Resource identifier
     */
    public async getProductById(
        id: string,
        options: { headers: { [name: string]: string } } = { headers: {} }
    ): Promise<{ response: http.IncomingMessage; body: Product }> {
        const localVariablePath =
            this.basePath +
            '/products/{id}'.replace('{' + 'id' + '}', encodeURIComponent(String(id)));
        const localVariableQueryParameters: any = {};
        const localVariableHeaderParameters: any = (<any>Object).assign({}, this._defaultHeaders);
        const produces = ['application/json'];
        // give precedence to 'application/json'
        localVariableHeaderParameters.Accept = produces.includes('application/json')
            ? 'application/json'
            : produces.join(',');
        const localVariableFormParameters: any = {};

        // verify required parameter 'id' is not null or undefined
        if (id === null || id === undefined) {
            throw new Error(
                'Required parameter id was null or undefined when calling getProductById.'
            );
        }

        (<any>Object).assign(localVariableHeaderParameters, options.headers);

        const localVariableUseFormData = false;

        const localVariableRequestOptions: localVarRequest.Options = {
            method: 'GET',
            qs: localVariableQueryParameters,
            headers: localVariableHeaderParameters,
            uri: localVariablePath,
            useQuerystring: this._useQuerystring,
            json: true
        };

        let authenticationPromise = Promise.resolve();
        authenticationPromise = authenticationPromise.then(() =>
            this.authentications.default.applyToRequest(localVariableRequestOptions)
        );

        let interceptorPromise = authenticationPromise;
        for (const interceptor of this.interceptors) {
            interceptorPromise = interceptorPromise.then(() =>
                interceptor(localVariableRequestOptions)
            );
        }

        return interceptorPromise.then(() => {
            if (Object.keys(localVariableFormParameters).length > 0) {
                if (localVariableUseFormData) {
                    (<any>localVariableRequestOptions).formData = localVariableFormParameters;
                } else {
                    localVariableRequestOptions.form = localVariableFormParameters;
                }
            }
            return new Promise<{ response: http.IncomingMessage; body: Product }>(
                (resolve, reject) => {
                    localVarRequest(localVariableRequestOptions, (error, response, body) => {
                        if (error) {
                            reject(error);
                        } else {
                            if (
                                response.statusCode &&
                                response.statusCode >= 200 &&
                                response.statusCode <= 299
                            ) {
                                body = ObjectSerializer.deserialize(body, 'Product');
                                resolve({ response: response, body: body });
                            } else {
                                reject(new HttpError(response, body, response.statusCode));
                            }
                        }
                    });
                }
            );
        });
    }
    /**
     * Returns a paginated list of products.
     * @summary List products (paginated)
     * @param productId
     * @param page 1-based page index
     * @param pageSize
     * @param text
     * @param minPrice
     * @param maxPrice
     */
    public async listProducts(
        productId?: string,
        page?: number,
        pageSize?: number,
        text?: string,
        minPrice?: number,
        maxPrice?: number,
        options: { headers: { [name: string]: string } } = { headers: {} }
    ): Promise<{ response: http.IncomingMessage; body: ProductsResponse }> {
        const localVariablePath = this.basePath + '/products';
        const localVariableQueryParameters: any = {};
        const localVariableHeaderParameters: any = (<any>Object).assign({}, this._defaultHeaders);
        const produces = ['application/json'];
        // give precedence to 'application/json'
        localVariableHeaderParameters.Accept = produces.includes('application/json')
            ? 'application/json'
            : produces.join(',');
        const localVariableFormParameters: any = {};

        if (productId !== undefined) {
            localVariableQueryParameters['productId'] = ObjectSerializer.serialize(
                productId,
                'string'
            );
        }

        if (page !== undefined) {
            localVariableQueryParameters['page'] = ObjectSerializer.serialize(page, 'number');
        }

        if (pageSize !== undefined) {
            localVariableQueryParameters['pageSize'] = ObjectSerializer.serialize(
                pageSize,
                'number'
            );
        }

        if (text !== undefined) {
            localVariableQueryParameters['text'] = ObjectSerializer.serialize(text, 'string');
        }

        if (minPrice !== undefined) {
            localVariableQueryParameters['minPrice'] = ObjectSerializer.serialize(
                minPrice,
                'number'
            );
        }

        if (maxPrice !== undefined) {
            localVariableQueryParameters['maxPrice'] = ObjectSerializer.serialize(
                maxPrice,
                'number'
            );
        }

        (<any>Object).assign(localVariableHeaderParameters, options.headers);

        const localVariableUseFormData = false;

        const localVariableRequestOptions: localVarRequest.Options = {
            method: 'GET',
            qs: localVariableQueryParameters,
            headers: localVariableHeaderParameters,
            uri: localVariablePath,
            useQuerystring: this._useQuerystring,
            json: true
        };

        let authenticationPromise = Promise.resolve();
        authenticationPromise = authenticationPromise.then(() =>
            this.authentications.default.applyToRequest(localVariableRequestOptions)
        );

        let interceptorPromise = authenticationPromise;
        for (const interceptor of this.interceptors) {
            interceptorPromise = interceptorPromise.then(() =>
                interceptor(localVariableRequestOptions)
            );
        }

        return interceptorPromise.then(() => {
            if (Object.keys(localVariableFormParameters).length > 0) {
                if (localVariableUseFormData) {
                    (<any>localVariableRequestOptions).formData = localVariableFormParameters;
                } else {
                    localVariableRequestOptions.form = localVariableFormParameters;
                }
            }
            return new Promise<{ response: http.IncomingMessage; body: ProductsResponse }>(
                (resolve, reject) => {
                    localVarRequest(localVariableRequestOptions, (error, response, body) => {
                        if (error) {
                            reject(error);
                        } else {
                            if (
                                response.statusCode &&
                                response.statusCode >= 200 &&
                                response.statusCode <= 299
                            ) {
                                body = ObjectSerializer.deserialize(body, 'ProductsResponse');
                                resolve({ response: response, body: body });
                            } else {
                                reject(new HttpError(response, body, response.statusCode));
                            }
                        }
                    });
                }
            );
        });
    }
    /**
     * Searches and filters products via a JSON request body. Functionally equivalent to `GET /products` with query parameters.
     * @summary Search products (DTO-friendly)
     * @param searchProductsRequest
     */
    public async searchProducts(
        searchProductsRequest: SearchProductsRequest,
        options: { headers: { [name: string]: string } } = { headers: {} }
    ): Promise<{ response: http.IncomingMessage; body: ProductsResponse }> {
        const localVariablePath = this.basePath + '/products/search';
        const localVariableQueryParameters: any = {};
        const localVariableHeaderParameters: any = (<any>Object).assign({}, this._defaultHeaders);
        const produces = ['application/json'];
        // give precedence to 'application/json'
        localVariableHeaderParameters.Accept = produces.includes('application/json')
            ? 'application/json'
            : produces.join(',');
        const localVariableFormParameters: any = {};

        // verify required parameter 'searchProductsRequest' is not null or undefined
        if (searchProductsRequest === null || searchProductsRequest === undefined) {
            throw new Error(
                'Required parameter searchProductsRequest was null or undefined when calling searchProducts.'
            );
        }

        (<any>Object).assign(localVariableHeaderParameters, options.headers);

        const localVariableUseFormData = false;

        const localVariableRequestOptions: localVarRequest.Options = {
            method: 'POST',
            qs: localVariableQueryParameters,
            headers: localVariableHeaderParameters,
            uri: localVariablePath,
            useQuerystring: this._useQuerystring,
            json: true,
            body: ObjectSerializer.serialize(searchProductsRequest, 'SearchProductsRequest')
        };

        let authenticationPromise = Promise.resolve();
        authenticationPromise = authenticationPromise.then(() =>
            this.authentications.default.applyToRequest(localVariableRequestOptions)
        );

        let interceptorPromise = authenticationPromise;
        for (const interceptor of this.interceptors) {
            interceptorPromise = interceptorPromise.then(() =>
                interceptor(localVariableRequestOptions)
            );
        }

        return interceptorPromise.then(() => {
            if (Object.keys(localVariableFormParameters).length > 0) {
                if (localVariableUseFormData) {
                    (<any>localVariableRequestOptions).formData = localVariableFormParameters;
                } else {
                    localVariableRequestOptions.form = localVariableFormParameters;
                }
            }
            return new Promise<{ response: http.IncomingMessage; body: ProductsResponse }>(
                (resolve, reject) => {
                    localVarRequest(localVariableRequestOptions, (error, response, body) => {
                        if (error) {
                            reject(error);
                        } else {
                            if (
                                response.statusCode &&
                                response.statusCode >= 200 &&
                                response.statusCode <= 299
                            ) {
                                body = ObjectSerializer.deserialize(body, 'ProductsResponse');
                                resolve({ response: response, body: body });
                            } else {
                                reject(new HttpError(response, body, response.statusCode));
                            }
                        }
                    });
                }
            );
        });
    }
    /**
     * Updates an existing product with optional image upload
     * @summary Edit product
     * @param id Resource identifier
     * @param title
     * @param price
     * @param description
     * @param active
     * @param imageUpload Optional product image
     */
    public async updateProduct(
        id: string,
        title: string,
        price: number,
        description?: string,
        active?: boolean,
        imageUpload?: RequestFile,
        options: { headers: { [name: string]: string } } = { headers: {} }
    ): Promise<{ response: http.IncomingMessage; body: Product }> {
        const localVariablePath = this.basePath + '/products';
        const localVariableQueryParameters: any = {};
        const localVariableHeaderParameters: any = (<any>Object).assign({}, this._defaultHeaders);
        const produces = ['application/json'];
        // give precedence to 'application/json'
        localVariableHeaderParameters.Accept = produces.includes('application/json')
            ? 'application/json'
            : produces.join(',');
        const localVariableFormParameters: any = {};

        // verify required parameter 'id' is not null or undefined
        if (id === null || id === undefined) {
            throw new Error(
                'Required parameter id was null or undefined when calling updateProduct.'
            );
        }

        // verify required parameter 'title' is not null or undefined
        if (title === null || title === undefined) {
            throw new Error(
                'Required parameter title was null or undefined when calling updateProduct.'
            );
        }

        // verify required parameter 'price' is not null or undefined
        if (price === null || price === undefined) {
            throw new Error(
                'Required parameter price was null or undefined when calling updateProduct.'
            );
        }

        (<any>Object).assign(localVariableHeaderParameters, options.headers);

        let localVariableUseFormData = false;

        if (id !== undefined) {
            localVariableFormParameters['id'] = ObjectSerializer.serialize(id, 'string');
        }

        if (title !== undefined) {
            localVariableFormParameters['title'] = ObjectSerializer.serialize(title, 'string');
        }

        if (description !== undefined) {
            localVariableFormParameters['description'] = ObjectSerializer.serialize(
                description,
                'string'
            );
        }

        if (price !== undefined) {
            localVariableFormParameters['price'] = ObjectSerializer.serialize(price, 'number');
        }

        if (active !== undefined) {
            localVariableFormParameters['active'] = ObjectSerializer.serialize(active, 'boolean');
        }

        if (imageUpload !== undefined) {
            localVariableFormParameters['imageUpload'] = imageUpload;
        }
        localVariableUseFormData = true;

        const localVariableRequestOptions: localVarRequest.Options = {
            method: 'PUT',
            qs: localVariableQueryParameters,
            headers: localVariableHeaderParameters,
            uri: localVariablePath,
            useQuerystring: this._useQuerystring,
            json: true
        };

        let authenticationPromise = Promise.resolve();
        if (this.authentications.bearerAuth.accessToken) {
            authenticationPromise = authenticationPromise.then(() =>
                this.authentications.bearerAuth.applyToRequest(localVariableRequestOptions)
            );
        }
        authenticationPromise = authenticationPromise.then(() =>
            this.authentications.default.applyToRequest(localVariableRequestOptions)
        );

        let interceptorPromise = authenticationPromise;
        for (const interceptor of this.interceptors) {
            interceptorPromise = interceptorPromise.then(() =>
                interceptor(localVariableRequestOptions)
            );
        }

        return interceptorPromise.then(() => {
            if (Object.keys(localVariableFormParameters).length > 0) {
                if (localVariableUseFormData) {
                    (<any>localVariableRequestOptions).formData = localVariableFormParameters;
                } else {
                    localVariableRequestOptions.form = localVariableFormParameters;
                }
            }
            return new Promise<{ response: http.IncomingMessage; body: Product }>(
                (resolve, reject) => {
                    localVarRequest(localVariableRequestOptions, (error, response, body) => {
                        if (error) {
                            reject(error);
                        } else {
                            if (
                                response.statusCode &&
                                response.statusCode >= 200 &&
                                response.statusCode <= 299
                            ) {
                                body = ObjectSerializer.deserialize(body, 'Product');
                                resolve({ response: response, body: body });
                            } else {
                                reject(new HttpError(response, body, response.statusCode));
                            }
                        }
                    });
                }
            );
        });
    }
    /**
     * Updates the product identified by `{id}` in the path with optional image upload. Functionally equivalent to `PUT /products` with the id in the body.
     * @summary Edit product
     * @param id Resource identifier
     * @param title
     * @param price
     * @param description
     * @param active
     * @param imageUpload Optional product image
     */
    public async updateProductById(
        id: string,
        title: string,
        price: number,
        description?: string,
        active?: boolean,
        imageUpload?: RequestFile,
        options: { headers: { [name: string]: string } } = { headers: {} }
    ): Promise<{ response: http.IncomingMessage; body: Product }> {
        const localVariablePath =
            this.basePath +
            '/products/{id}'.replace('{' + 'id' + '}', encodeURIComponent(String(id)));
        const localVariableQueryParameters: any = {};
        const localVariableHeaderParameters: any = (<any>Object).assign({}, this._defaultHeaders);
        const produces = ['application/json'];
        // give precedence to 'application/json'
        localVariableHeaderParameters.Accept = produces.includes('application/json')
            ? 'application/json'
            : produces.join(',');
        const localVariableFormParameters: any = {};

        // verify required parameter 'id' is not null or undefined
        if (id === null || id === undefined) {
            throw new Error(
                'Required parameter id was null or undefined when calling updateProductById.'
            );
        }

        // verify required parameter 'title' is not null or undefined
        if (title === null || title === undefined) {
            throw new Error(
                'Required parameter title was null or undefined when calling updateProductById.'
            );
        }

        // verify required parameter 'price' is not null or undefined
        if (price === null || price === undefined) {
            throw new Error(
                'Required parameter price was null or undefined when calling updateProductById.'
            );
        }

        (<any>Object).assign(localVariableHeaderParameters, options.headers);

        let localVariableUseFormData = false;

        if (title !== undefined) {
            localVariableFormParameters['title'] = ObjectSerializer.serialize(title, 'string');
        }

        if (description !== undefined) {
            localVariableFormParameters['description'] = ObjectSerializer.serialize(
                description,
                'string'
            );
        }

        if (price !== undefined) {
            localVariableFormParameters['price'] = ObjectSerializer.serialize(price, 'number');
        }

        if (active !== undefined) {
            localVariableFormParameters['active'] = ObjectSerializer.serialize(active, 'boolean');
        }

        if (imageUpload !== undefined) {
            localVariableFormParameters['imageUpload'] = imageUpload;
        }
        localVariableUseFormData = true;

        const localVariableRequestOptions: localVarRequest.Options = {
            method: 'PUT',
            qs: localVariableQueryParameters,
            headers: localVariableHeaderParameters,
            uri: localVariablePath,
            useQuerystring: this._useQuerystring,
            json: true
        };

        let authenticationPromise = Promise.resolve();
        if (this.authentications.bearerAuth.accessToken) {
            authenticationPromise = authenticationPromise.then(() =>
                this.authentications.bearerAuth.applyToRequest(localVariableRequestOptions)
            );
        }
        authenticationPromise = authenticationPromise.then(() =>
            this.authentications.default.applyToRequest(localVariableRequestOptions)
        );

        let interceptorPromise = authenticationPromise;
        for (const interceptor of this.interceptors) {
            interceptorPromise = interceptorPromise.then(() =>
                interceptor(localVariableRequestOptions)
            );
        }

        return interceptorPromise.then(() => {
            if (Object.keys(localVariableFormParameters).length > 0) {
                if (localVariableUseFormData) {
                    (<any>localVariableRequestOptions).formData = localVariableFormParameters;
                } else {
                    localVariableRequestOptions.form = localVariableFormParameters;
                }
            }
            return new Promise<{ response: http.IncomingMessage; body: Product }>(
                (resolve, reject) => {
                    localVarRequest(localVariableRequestOptions, (error, response, body) => {
                        if (error) {
                            reject(error);
                        } else {
                            if (
                                response.statusCode &&
                                response.statusCode >= 200 &&
                                response.statusCode <= 299
                            ) {
                                body = ObjectSerializer.deserialize(body, 'Product');
                                resolve({ response: response, body: body });
                            } else {
                                reject(new HttpError(response, body, response.statusCode));
                            }
                        }
                    });
                }
            );
        });
    }
}
