<!DOCTYPE html>
<html lang="<%= locale %>">
<%- include("../layouts/pdf-template-head.ejs"); %>
<body>
<main class="centered">
    <h1><%= title %></h1>
    <ul>
        <% lines.forEach(function(line) { %>
            <li><%= line %></li>
        <% }); %>
    </ul>
</main>
</body>
</html>
