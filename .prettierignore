api
.prism
contract.bruno.yml
contract.insomnia.json
contract.mockoon.json
contract.postman.json
src/types/asyncapi.generated.ts

# The REST contract. `redocly bundle` decides its layout now — 2-space, its own key order — and
# `check:contracts-bundle` asserts the committed bytes equal a fresh run. Prettier reformatting it
# afterwards would make that check fail on a file nobody edited. The MODULE documents it is built
# from are formatted as normal.
openapi.yaml

# Stryker's own incremental cache — gitignored (see `.gitignore`) but still present on disk
# between local runs, and written by Stryker, not by anyone typing JSON. Same reasoning as the
# file above: reformatting a file only a tool writes fights the tool that owns it.
tmp/reports/stryker-incremental.json

# Working directories that belong to tooling rather than to this codebase, and to whoever is
# running it rather than to this repo's formatting rules. Prettier scans the whole tree, so an
# unformatted scratch directory here fails `prettier:check` — which is in `npm run complete`, which
# the pre-commit hook runs. One person's working notes would otherwise block every commit in every
# concurrent session, and the way out people find is `--no-verify`, which costs the whole gate.
.claude/
CLAUDE.md

# The three shared authorization artefacts. Committed with identical bytes in the PHP twin, which
# formats YAML with dprint rather than Prettier. Two formatters over one shared file is a silent
# fork — the same reasoning that excludes the Spectral rulesets over there.
shared/authorization-keys.yaml
shared/authorization-roles.yaml
shared/authorization-conformance.yaml

# The same working notes `.gitignore` excludes. Prettier does not read `.gitignore`, so without
# this an untracked plan doc still fails `prettier:check` — a formatting gate on a file that is
# not in the repo and never will be.
#
# `CLAUDE.md` is NOT re-included below, unlike in `.gitignore`: it is committed, but it was
# already exempt from formatting above and must stay that way.
/*.md
!/README.md
