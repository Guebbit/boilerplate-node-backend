        # ---------- products — the schemas only this module's paths reference. ----------
        ProductEnvelope:
            type: object
            additionalProperties: false
            required: [success, status, message, data]
            properties:
                success: *envelopeSuccess
                status: *envelopeStatus
                message: *envelopeMessage
                data:
                    $ref: '#/components/schemas/Product'

        ProductsResponseEnvelope:
            type: object
            additionalProperties: false
            required: [success, status, message, data]
            properties:
                success: *envelopeSuccess
                status: *envelopeStatus
                message: *envelopeMessage
                data:
                    $ref: '#/components/schemas/ProductsResponse'

        # One value of `categories`/`tags`, with how many PUBLIC products carry it. Counts
        # follow `publicScope` — a category held only by hidden products does not exist to the
        # storefront, and a filter chip that finds nothing is worse than no chip.
        FacetCount:
            type: object
            additionalProperties: false
            required: [name, count]
            properties:
                name:
                    type: string
                count:
                    type: integer
                    minimum: 1

        CatalogueFacetsResponse:
            type: object
            additionalProperties: false
            required: [categories, tags]
            properties:
                categories:
                    type: array
                    items:
                        $ref: '#/components/schemas/FacetCount'
                tags:
                    type: array
                    items:
                        $ref: '#/components/schemas/FacetCount'

        CatalogueFacetsEnvelope:
            type: object
            additionalProperties: false
            required: [success, status, message, data]
            properties:
                success: *envelopeSuccess
                status: *envelopeStatus
                message: *envelopeMessage
                data:
                    $ref: '#/components/schemas/CatalogueFacetsResponse'

        SearchProductsRequest:
            type: object
            additionalProperties: false
            properties:
                page:
                    $ref: '#/components/schemas/Page'
                pageSize:
                    $ref: '#/components/schemas/PageSize'
                text:
                    $ref: '#/components/schemas/Text'
                id:
                    $ref: '#/components/schemas/Id'
                minPrice:
                    type: number
                    format: double
                    minimum: 0
                maxPrice:
                    type: number
                    format: double
                    minimum: 0
                category:
                    type: string
                tag:
                    type: string

        CreateProductRequest:
            type: object
            additionalProperties: false
            required: [title, price]
            properties:
                title:
                    type: string
                price:
                    type: number
                    format: double
                    minimum: 0
                # The demo default. Declared for the same reason `active`'s is (see below): an
                # undeclared default is one the paired frontend's mock has to guess at. `100`
                # rather than `0` because a boilerplate demo's new product exists to be bought;
                # a real shop lowers it to 0 and stocks deliberately.
                stock:
                    type: integer
                    minimum: 0
                    default: 100
                description:
                    type: string
                # ---------- On `active`, and why it is declared here but not on the updates ----------
                #
                # `active` and `deletedAt` are INDEPENDENT. A record can be active or not regardless
                # of whether it has been soft-deleted, and the two are stored separately. What they
                # share is an effect, not a value: a non-admin sees a record only when it is active
                # AND not soft-deleted (`publicScope()` in the products repository requires both).
                # So a soft-deleted record behaves exactly like an inactive one from outside, while
                # remaining a distinct state inside.
                #
                # The default is declared rather than left to each implementation. Undeclared, the
                # API and the paired frontend's mock each pick their own, and the same request
                # yields a publicly visible product against one and a hidden one against the
                # other — a disagreement no contract test on either side can see.
                #
                # It appears on the CREATE bodies only. On an update, an omitted `active` means
                # "leave it as it is" — a default there would silently republish a deactivated
                # record on any unrelated edit.
                active:
                    type: boolean
                    default: true
                imageUrl:
                    $ref: '#/components/schemas/ImageUrl'
                categories:
                    type: array
                    items:
                        type: string
                tags:
                    type: array
                    items:
                        type: string

        CreateProductRequestMultipart:
            type: object
            additionalProperties: false
            required: [title, price]
            properties:
                title:
                    type: string
                price:
                    type: number
                    format: double
                    minimum: 0
                # The demo default. Declared for the same reason `active`'s is (see below): an
                # undeclared default is one the paired frontend's mock has to guess at. `100`
                # rather than `0` because a boilerplate demo's new product exists to be bought;
                # a real shop lowers it to 0 and stocks deliberately.
                stock:
                    type: integer
                    minimum: 0
                    default: 100
                description:
                    type: string
                # Same default, same reasoning as `CreateProductRequest.active` above.
                active:
                    type: boolean
                    default: true
                imageUpload:
                    # No maxLength/contentMediaType by design — see the note under `ImageUrl`.
                    type: string
                    format: binary
                    description: Optional product image
                categories:
                    type: array
                    items:
                        type: string
                tags:
                    type: array
                    items:
                        type: string

        UpdateProductRequest:
            type: object
            additionalProperties: false
            required: [id, title, price]
            properties:
                id:
                    $ref: '#/components/schemas/Id'
                title:
                    type: string
                description:
                    type: string
                price:
                    type: number
                    format: double
                    minimum: 0
                # No default on an update: an omitted `stock` means "leave it as it is".
                stock:
                    type: integer
                    minimum: 0
                active:
                    type: boolean
                imageUrl:
                    $ref: '#/components/schemas/ImageUrl'
                categories:
                    type: array
                    items:
                        type: string
                tags:
                    type: array
                    items:
                        type: string

        UpdateProductRequestMultipart:
            type: object
            additionalProperties: false
            required: [id, title, price]
            properties:
                id:
                    $ref: '#/components/schemas/Id'
                title:
                    type: string
                description:
                    type: string
                price:
                    type: number
                    format: double
                    minimum: 0
                # No default on an update: an omitted `stock` means "leave it as it is".
                stock:
                    type: integer
                    minimum: 0
                active:
                    type: boolean
                imageUpload:
                    # No maxLength/contentMediaType by design — see the note under `ImageUrl`.
                    type: string
                    format: binary
                    description: Optional product image
                categories:
                    type: array
                    items:
                        type: string
                tags:
                    type: array
                    items:
                        type: string

        UpdateProductByIdRequest:
            type: object
            additionalProperties: false
            required: [title, price]
            properties:
                title:
                    type: string
                description:
                    type: string
                price:
                    type: number
                    format: double
                    minimum: 0
                # No default on an update: an omitted `stock` means "leave it as it is".
                stock:
                    type: integer
                    minimum: 0
                active:
                    type: boolean
                imageUrl:
                    $ref: '#/components/schemas/ImageUrl'
                categories:
                    type: array
                    items:
                        type: string
                tags:
                    type: array
                    items:
                        type: string

        UpdateProductByIdRequestMultipart:
            type: object
            additionalProperties: false
            required: [title, price]
            properties:
                title:
                    type: string
                description:
                    type: string
                price:
                    type: number
                    format: double
                    minimum: 0
                # No default on an update: an omitted `stock` means "leave it as it is".
                stock:
                    type: integer
                    minimum: 0
                active:
                    type: boolean
                imageUpload:
                    # No maxLength/contentMediaType by design — see the note under `ImageUrl`.
                    type: string
                    format: binary
                    description: Optional product image
                categories:
                    type: array
                    items:
                        type: string
                tags:
                    type: array
                    items:
                        type: string

        DeleteProductRequest:
            type: object
            additionalProperties: false
            required: [id]
            properties:
                id:
                    $ref: '#/components/schemas/Id'
                hardDelete:
                    type: boolean
                    default: false

        ProductsResponse:
            type: object
            additionalProperties: false
            required: [items, meta]
            properties:
                items:
                    type: array
                    items:
                        $ref: '#/components/schemas/Product'
                meta:
                    $ref: '#/components/schemas/PaginationMeta'

