    /products:
        get:
            tags: [Products]
            summary: List products (paginated)
            description: Returns a paginated list of products.
            operationId: listProducts
            parameters:
                - $ref: '#/components/parameters/PageParam'
                - $ref: '#/components/parameters/PageSizeParam'
                - $ref: '#/components/parameters/TextParam'
                - $ref: '#/components/parameters/IdParam'
                # Declared here as well as in SearchProductsRequest: the controller merges query
                # and body, so `GET /products?category=x` and `POST /products/search {category}`
                # are one filter reached two ways.
                - name: category
                  in: query
                  required: false
                  schema:
                      type: string
                - name: tag
                  in: query
                  required: false
                  schema:
                      type: string
                - name: minPrice
                  in: query
                  required: false
                  schema:
                      type: number
                      format: double
                      minimum: 0
                - name: maxPrice
                  in: query
                  required: false
                  schema:
                      type: number
                      format: double
                      minimum: 0
            responses:
                '200':
                    description: Products page
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ProductsResponseEnvelope'
                '422': { $ref: '#/components/responses/ValidationError' }
                '500': { $ref: '#/components/responses/InternalError' }
        post:
            tags: [Products]
            summary: Create product
            description: Creates a new product with optional image upload
            operationId: createProduct
            security:
                - bearerAuth: []
            requestBody:
                required: true
                content:
                    application/json:
                        schema:
                            $ref: '#/components/schemas/CreateProductRequest'
                    multipart/form-data:
                        schema:
                            $ref: '#/components/schemas/CreateProductRequestMultipart'
            responses:
                '201':
                    description: Created product
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ProductEnvelope'
                '401': { $ref: '#/components/responses/Unauthorized' }
                '403': { $ref: '#/components/responses/Forbidden' }
                '422': { $ref: '#/components/responses/ValidationError' }
                '500': { $ref: '#/components/responses/InternalError' }
        put:
            tags: [Products]
            summary: Edit product
            description: Updates an existing product with optional image upload
            operationId: updateProduct
            security:
                - bearerAuth: []
            requestBody:
                required: true
                content:
                    application/json:
                        schema:
                            $ref: '#/components/schemas/UpdateProductRequest'
                    multipart/form-data:
                        schema:
                            $ref: '#/components/schemas/UpdateProductRequestMultipart'
            responses:
                '200':
                    description: Updated product
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ProductEnvelope'
                '401': { $ref: '#/components/responses/Unauthorized' }
                '403': { $ref: '#/components/responses/Forbidden' }
                '404': { $ref: '#/components/responses/NotFound' }
                '422': { $ref: '#/components/responses/ValidationError' }
                '500': { $ref: '#/components/responses/InternalError' }
        delete:
            tags: [Products]
            summary: Delete product
            description: Deletes the product identified by the `id` field in the request body. Set `hardDelete` to `true` to permanently remove the record
            operationId: deleteProduct
            security:
                - bearerAuth: []
            requestBody:
                required: true
                content:
                    application/json:
                        schema:
                            $ref: '#/components/schemas/DeleteProductRequest'
            responses:
                '200': { $ref: '#/components/responses/Success' }
                '401': { $ref: '#/components/responses/Unauthorized' }
                '403': { $ref: '#/components/responses/Forbidden' }
                '404': { $ref: '#/components/responses/NotFound' }
                '422': { $ref: '#/components/responses/ValidationError' }
                '500': { $ref: '#/components/responses/InternalError' }

    /products/categories:
        get:
            tags: [Products]
            summary: Catalogue facets
            description: Every category and tag the PUBLIC catalogue carries, each with how many visible products hold it — what a storefront renders as filter chips. Sorted by count descending, then name. Counts follow the same visibility rule the listing does, so a chip can never lead to an empty page.
            operationId: getCatalogueFacets
            security: []
            responses:
                '200':
                    description: Categories and tags with counts
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/CatalogueFacetsEnvelope'
                '500': { $ref: '#/components/responses/InternalError' }

    /products/{id}:
        # Same as /products/id={id}
        get:
            tags: [Products]
            summary: Product details
            description: Returns the full details of the product identified by `{id}`. Functionally equivalent to `GET /products?id={id}`.
            operationId: getProductById
            parameters:
                - $ref: '#/components/parameters/IdPathParam'
            responses:
                '200':
                    description: Product
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ProductEnvelope'
                '404': { $ref: '#/components/responses/NotFound' }
                '500': { $ref: '#/components/responses/InternalError' }
        # Same as PUT /products
        put:
            tags: [Products]
            summary: Edit product
            description: Updates the product identified by `{id}` in the path with optional image upload. Functionally equivalent to `PUT /products` with the id in the body.
            operationId: updateProductById
            security:
                - bearerAuth: []
            parameters:
                - $ref: '#/components/parameters/IdPathParam'
            requestBody:
                required: true
                content:
                    application/json:
                        schema:
                            $ref: '#/components/schemas/UpdateProductByIdRequest'
                    multipart/form-data:
                        schema:
                            $ref: '#/components/schemas/UpdateProductByIdRequestMultipart'
            responses:
                '200':
                    description: Updated product
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ProductEnvelope'
                '401': { $ref: '#/components/responses/Unauthorized' }
                '422': { $ref: '#/components/responses/ValidationError' }
                '404': { $ref: '#/components/responses/NotFound' }
                '500': { $ref: '#/components/responses/InternalError' }
        # Same as DELETE /products
        delete:
            tags: [Products]
            summary: Delete product
            description: Deletes the product identified by `{id}` in the path. Pass the `hardDelete` query parameter as `true` to permanently remove the record. Functionally equivalent to `DELETE /products`.
            operationId: deleteProductById
            security:
                - bearerAuth: []
            parameters:
                - $ref: '#/components/parameters/IdPathParam'
                - name: hardDelete
                  in: query
                  schema:
                      type: boolean
            requestBody:
                required: false
                content:
                    application/json:
                        schema:
                            $ref: '#/components/schemas/HardDeleteRequest'
            responses:
                '200': { $ref: '#/components/responses/Success' }
                '401': { $ref: '#/components/responses/Unauthorized' }
                '404': { $ref: '#/components/responses/NotFound' }
                '422': { $ref: '#/components/responses/ValidationError' }
                '500': { $ref: '#/components/responses/InternalError' }

    # Same as DELETE /products/{id}?hardDelete=true — the flag spelled in the path instead of
    # in the query. Both reach the same handler; neither is the canonical one.
    /products/{id}/hard:
        delete:
            tags: [Products]
            summary: Permanently delete product
            description: Permanently removes the product identified by `{id}`, rather than soft-deleting it. Functionally equivalent to `DELETE /products/{id}?hardDelete=true`.
            operationId: hardDeleteProductById
            security:
                - bearerAuth: []
            parameters:
                - $ref: '#/components/parameters/IdPathParam'
            responses:
                '200': { $ref: '#/components/responses/Success' }
                '401': { $ref: '#/components/responses/Unauthorized' }
                '404': { $ref: '#/components/responses/NotFound' }
                '422': { $ref: '#/components/responses/ValidationError' }
                '500': { $ref: '#/components/responses/InternalError' }

    # Same as GET /products, but with JSON body, more suitable for DTOs and multi-language codegen
    /products/search:
        post:
            tags: [Products]
            summary: Search products (DTO-friendly)
            description: Searches and filters products via a JSON request body. Functionally equivalent to `GET /products` with query parameters.
            operationId: searchProducts
            requestBody:
                required: true
                content:
                    application/json:
                        schema:
                            $ref: '#/components/schemas/SearchProductsRequest'
            responses:
                '200':
                    description: Products search results
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ProductsResponseEnvelope'
                '422': { $ref: '#/components/responses/ValidationError' }
                '500': { $ref: '#/components/responses/InternalError' }

