/**
 * @module
 * Addresses — public barrel; the only surface a sibling module may import (see
 * `docs/theory/strategic-ddd.md` §5 for the rule). See docs/modules/addresses.md.
 */

export * from './service';

export type * from './model';
