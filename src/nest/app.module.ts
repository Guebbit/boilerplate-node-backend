import { Module } from '@nestjs/common';

/**
 * Nest root module.
 *
 * We keep this intentionally small because this repository is a boilerplate.
 * Future projects can add feature modules, providers, and controllers from here.
 */
@Module({})
export class AppModule {}
